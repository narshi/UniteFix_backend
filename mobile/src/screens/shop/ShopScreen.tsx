/**
 * Shop Screen — HALTED (Coming Soon)
 * Product ordering is paused per AI_CONTEXT.md §3.K
 * Will resume with Annual Maintenance Package feature.
 */

import React from 'react';
import { ComingSoonOverlay } from './ComingSoonOverlay';

export function ShopScreen() {
    return <ComingSoonOverlay />;
}
